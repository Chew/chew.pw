<h1>Possible Words</h1>

<p>Found <%= @wordles.count %> possible words.</p>

<%= link_to button_tag("#{fas_icon "home"} Back".html_safe, class: "btn btn-primary"), "/util/wordle" %>
<br>
<hr>

<div class="row">
  <% if @wordles.empty? %>
    <p>No possible words found for input! Try again?</p>
  <% else %>
    <% @wordles.each do |word| %>
      <div class="col wordle-guess">
        <%= word %>
      </div>
    <% end %>
  <% end %>
</div>